# SPDX-License-Identifier: Apache-2.0
# Genesis V2 alias — 27B INT4 fp8kv long-context (256K) on 2× A5000.
#
# ⚠ PIN NOTE (2026-06-11 fleet validation): the
# qwen3.6-27b-int4-autoround-fp8kv ModelDef pin_holds 626fa9bb
# (versions.pin_hold — fp8kv KV variant not covered by the 0.22.1 fleet
# validation), while the a5000-2x hardware YAML image is the promoted
# 0.22.1 pin (303916e93). Launching this preset on the current image is
# UNVALIDATED until an fp8kv smoke runs on 0.22.1; :nightly-previous
# (626fa9bb) is retained on the rig for exactly this case (CLAUDE.md
# pin policy).

model: qwen3.6-27b-int4-autoround-fp8kv
hardware: a5000-2x-24gbvram-16cpu-128gbram
profile: qwen3.6-27b-long-ctx

card:
  title: "Qwen 3.6 27B INT4 FP8 KV — long-context 280K probe (2× A5000)"
  summary: |
    Long-context preset on the fp8_e5m2 KV variant: 280K context
    ceiling probe on 2× A5000. fp8 KV is preferred over TQ k8v4 at
    extreme context because TQ workspace overhead grows non-linearly
    past ~200K. Bench-pending status — long-ctx 32K+ refresh deferred
    per profile note. Use prod-qwen3.6-27b-tq-k8v4 for typical (<131K) workloads.
  status: bench_pending
  audience: bench
  maturity: validated
  mode: long_context
  workload_allow:
    - long_context_bench
    - long_context_research
  workload_deny:
    - free_chat
    - structured_json.short
    - tool_call.short
  concurrency:
    min: 1
    max: 2
    canonical: 2
  K: 3
  context:
    max_model_len: 280000
    typical_input_tokens: 32768
    typical_output_tokens: 4096
  routing_family: qwen3_6_27b_int4_fp8kv_long_ctx
  default_for_family: true
  fallback_preset: prod-qwen3.6-27b-tq-k8v4
